import { Download, FileCheck2 } from "lucide-react";
import Button from "../../components/Button";

const rows = [{ name: "Alex Santos", event: "IT Week 2026", status: "Attended" }, { name: "Mika Reyes", event: "Innovation Summit 2026", status: "Registered" }, { name: "Jordan Cruz", event: "Cybersecurity Awareness Seminar", status: "Attended" }];
function AttendanceWorkspace() { const exportCsv = () => { const csv = ["Name,Event,Status", ...rows.map((row) => `${row.name},${row.event},${row.status}`)].join("\n"); const url = URL.createObjectURL(new Blob([csv], { type: "text/csv" })); const link = document.createElement("a"); link.href = url; link.download = "attendance.csv"; link.click(); URL.revokeObjectURL(url); }; return <div><div className="page-heading"><div><p className="eyebrow">Organizer workspace</p><h1>Attendance</h1><p className="muted">Review mock registration states and export a CSV report.</p></div><Button onClick={exportCsv}><Download size={16} /> Export CSV</Button></div><div className="table-card"><div className="responsive-table"><table><thead><tr><th>Student</th><th>Event</th><th>Status</th></tr></thead><tbody>{rows.map((row) => <tr key={`${row.name}-${row.event}`}><td>{row.name}</td><td>{row.event}</td><td><span className={`status ${row.status.toLowerCase()}`}><FileCheck2 size={14} />{row.status}</span></td></tr>)}</tbody></table></div></div></div>; }
export default AttendanceWorkspace;
